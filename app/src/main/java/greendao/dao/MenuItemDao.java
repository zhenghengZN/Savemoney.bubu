package greendao.dao;

import android.database.Cursor;

import java.util.ArrayList;
import java.util.List;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.database.Database;
import de.greenrobot.dao.database.DatabaseStatement;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;
import greendao.bean.MenuItem;
import greendao.bean.MenuSection;


// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.

/**
 * DAO for table "MENU_ITEM".
*/
public class MenuItemDao extends AbstractDao<MenuItem, String> {

    public static final String TABLENAME = "MENU_ITEM";

    /**
     * Properties of entity MenuItem.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property ObjectId = new Property(0, String.class, "objectId", true, "OBJECT_ID");
        public final static Property Desc = new Property(1, String.class, "desc", false, "DESC");
        public final static Property DescCn = new Property(2, String.class, "descCn", false, "DESC_CN");
        public final static Property Title = new Property(3, String.class, "title", false, "TITLE");
        public final static Property TitleCn = new Property(4, String.class, "titleCn", false, "TITLE_CN");
        public final static Property Price = new Property(5, String.class, "price", false, "PRICE");
        public final static Property Section = new Property(6, String.class, "section", false, "SECTION");
        public final static Property Options = new Property(7, String.class, "options", false, "OPTIONS");
        public final static Property RowStatus = new Property(8, String.class, "rowStatus", false, "ROW_STATUS");
        public final static Property CreatedAt = new Property(9, String.class, "createdAt", false, "CREATED_AT");
        public final static Property UpdatedAt = new Property(10, String.class, "updatedAt", false, "UPDATED_AT");
        public final static Property MenuSectionId = new Property(11, String.class, "menuSectionId", false, "MENU_SECTION_ID");
    };

    private DaoSession daoSession;

    private Query<MenuItem> menuSection_MenuItemListQuery;

    public MenuItemDao(DaoConfig config) {
        super(config);
    }
    
    public MenuItemDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"MENU_ITEM\" (" + //
                "\"OBJECT_ID\" TEXT PRIMARY KEY NOT NULL ," + // 0: objectId
                "\"DESC\" TEXT," + // 1: desc
                "\"DESC_CN\" TEXT," + // 2: descCn
                "\"TITLE\" TEXT," + // 3: title
                "\"TITLE_CN\" TEXT," + // 4: titleCn
                "\"PRICE\" TEXT," + // 5: price
                "\"SECTION\" TEXT," + // 6: section
                "\"OPTIONS\" TEXT," + // 7: options
                "\"ROW_STATUS\" TEXT," + // 8: rowStatus
                "\"CREATED_AT\" TEXT," + // 9: createdAt
                "\"UPDATED_AT\" TEXT," + // 10: updatedAt
                "\"MENU_SECTION_ID\" TEXT);"); // 11: menuSectionId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"MENU_ITEM\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(DatabaseStatement stmt, MenuItem entity) {
        stmt.clearBindings();
        stmt.bindString(1, entity.getObjectId());
 
        String desc = entity.getDesc();
        if (desc != null) {
            stmt.bindString(2, desc);
        }
 
        String descCn = entity.getDescCn();
        if (descCn != null) {
            stmt.bindString(3, descCn);
        }
 
        String title = entity.getTitle();
        if (title != null) {
            stmt.bindString(4, title);
        }
 
        String titleCn = entity.getTitleCn();
        if (titleCn != null) {
            stmt.bindString(5, titleCn);
        }
 
        String price = entity.getPrice();
        if (price != null) {
            stmt.bindString(6, price);
        }
 
        String section = entity.getSection();
        if (section != null) {
            stmt.bindString(7, section);
        }
 
        String options = entity.getOptions();
        if (options != null) {
            stmt.bindString(8, options);
        }
 
        String rowStatus = entity.getRowStatus();
        if (rowStatus != null) {
            stmt.bindString(9, rowStatus);
        }
 
        String createdAt = entity.getCreatedAt();
        if (createdAt != null) {
            stmt.bindString(10, createdAt);
        }
 
        String updatedAt = entity.getUpdatedAt();
        if (updatedAt != null) {
            stmt.bindString(11, updatedAt);
        }
 
        String menuSectionId = entity.getMenuSectionId();
        if (menuSectionId != null) {
            stmt.bindString(12, menuSectionId);
        }
    }

    @Override
    protected void attachEntity(MenuItem entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.getString(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public MenuItem readEntity(Cursor cursor, int offset) {
        MenuItem entity = new MenuItem( //
            cursor.getString(offset + 0), // objectId
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // desc
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // descCn
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // title
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // titleCn
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // price
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // section
            cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7), // options
            cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8), // rowStatus
            cursor.isNull(offset + 9) ? null : cursor.getString(offset + 9), // createdAt
            cursor.isNull(offset + 10) ? null : cursor.getString(offset + 10), // updatedAt
            cursor.isNull(offset + 11) ? null : cursor.getString(offset + 11) // menuSectionId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, MenuItem entity, int offset) {
        entity.setObjectId(cursor.getString(offset + 0));
        entity.setDesc(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setDescCn(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setTitle(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setTitleCn(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setPrice(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setSection(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
        entity.setOptions(cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7));
        entity.setRowStatus(cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8));
        entity.setCreatedAt(cursor.isNull(offset + 9) ? null : cursor.getString(offset + 9));
        entity.setUpdatedAt(cursor.isNull(offset + 10) ? null : cursor.getString(offset + 10));
        entity.setMenuSectionId(cursor.isNull(offset + 11) ? null : cursor.getString(offset + 11));
     }
    
    /** @inheritdoc */
    @Override
    protected String updateKeyAfterInsert(MenuItem entity, long rowId) {
        return entity.getObjectId();
    }
    
    /** @inheritdoc */
    @Override
    public String getKey(MenuItem entity) {
        if(entity != null) {
            return entity.getObjectId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "menuItemList" to-many relationship of MenuSection. */
    public List<MenuItem> _queryMenuSection_MenuItemList(String menuSectionId) {
        synchronized (this) {
            if (menuSection_MenuItemListQuery == null) {
                QueryBuilder<MenuItem> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.MenuSectionId.eq(null));
                menuSection_MenuItemListQuery = queryBuilder.build();
            }
        }
        Query<MenuItem> query = menuSection_MenuItemListQuery.forCurrentThread();
        query.setParameter(0, menuSectionId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getMenuSectionDao().getAllColumns());
            builder.append(" FROM MENU_ITEM T");
            builder.append(" LEFT JOIN MENU_SECTION T0 ON T.\"MENU_SECTION_ID\"=T0.\"OBJECT_ID\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected MenuItem loadCurrentDeep(Cursor cursor, boolean lock) {
        MenuItem entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        MenuSection menuSection = loadCurrentOther(daoSession.getMenuSectionDao(), cursor, offset);
        entity.setMenuSection(menuSection);

        return entity;    
    }

    public MenuItem loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<MenuItem> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<MenuItem> list = new ArrayList<MenuItem>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<MenuItem> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<MenuItem> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
